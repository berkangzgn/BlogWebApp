<div class="row">
  <div class="col-xs-2 col-md-offset-3">
    <%= gravatar_for @user, size: 130 %>
  </div>

  <div>
    <h1 ><%= @user.username %>'s Articles</h1>
  </div>
</div><hr>

<div class="center">
  <%= will_paginate @user_articles %>
</div>

<%= render 'articles/article', obj: @user_articles %>
<div class="center">
  <%= will_paginate @user_articles %>
</div>
<div class="footer"><a href="<%= @user.email %>" >Send <%= @user.username %> an e-mail.</a></div>
